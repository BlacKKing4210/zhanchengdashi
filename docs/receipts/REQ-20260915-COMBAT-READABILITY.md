# Combat readability corrections — engineering receipt

- READY. Actual project D:/AI/zhanchengdashi, baseline e21386e, Godot 4.6.2 / tabs. Producer user-producer; accountable owner and runtime writer codex-primary. Current user asks deck-only card notifications, reliable/polished ranged projectiles, closer melee attacks and overhead animal health bars. Preserve dirty design/战斗数值.xlsx and unrelated artifacts. No active runtime lock conflict. Independent combat audit completed; ranged_audit_sep15 owns only the two new test files, root owns runtime and receipts.
- RAG READY / GLOBAL / REQ-20260915-COMBAT-READABILITY, index 061e9955541bedba4c636d890f8f88d8c6d50396f4fe82e4958e3cfdac72fbeb. Source: current main.gd, animal_skill_runtime.gd, approved B-style UI and previous combat feedback receipt. Primary Godot feature slice; orchestrator/control-plane/RAG, UI priority, visual quality and supplementary game defaults. Focused correction, no new art direction, asset family or editable-design tool requirement.
- Acceptance: notifications only in SCREEN_DECK for both card drawing routes. Animal HP bars and team markers above the actual transformed sprite rectangle with a visible gap through rarity/camera zoom/jump/attack poses; building bars unchanged. Fix missing existing ranged feedback paths and improve readable projectile head/trail/impact, preserving damage/hit timing/range/bounce/pierce. Move melee threshold closer, initially target 0.65 cell instead of 1.01, with explicit jump/parity regression so short range never strands jumpers. No card-stat edits or unapproved ranged conversion.
- Write set main.gd, animal_skill_runtime.gd, targeted tests/test_combat_readability.gd/.tscn and necessary adjacent assertions, CURRENT_GAME_DESIGN, active_scope and this receipt. Non-goals: account/cloud, reward/economy, story, card CSV/workbook, new APK. Evidence temp/qa/combat-readability-20260915, isolated no-auto-login harness; 720x1280 and 360x640, repeated frame stepping, all declared ranged animals, melee pursuit and jump/building reach, red-dot pixels, overhead bar bounds. Cleanup test data excluded from runtime exports, release own lock after scoped verification, commit and push. Fingerprint GLOBAL/20260915/combat-readability/codex-primary/main+animal-runtime+tests. reusable_method_candidate: none.
- Audit decision: ordinary melee contact becomes 0.65 cell. Fixed 2/3-cell jumpers retain adjacent-cell logical reach and use a short visual contact lunge; no extra walking, changed landing/capture events or unreachable parity loops. Projectile presentation gets a stable per-flight ID and local playback clock so 0.20s snapshots can replay a shorter flight once; authoritative damage, speed, bounce/pierce and target resolution remain untouched.

## Implemented correction and verification scope

- Red-dot eligibility is centralized at `_card_upgrade_dot_visible`: SCREEN_DECK plus the unchanged real upgrade eligibility. Both normal and clipped card draws share it. No gacha/detail interaction or card-count rule changes.
- Animal health bars use the rendered texture/frame's cached alpha bounds, then the exact draw pose (offset, rarity/art scale, mirroring and rotation). Their lower edge stays 8 logical pixels above visible artwork. Transparent padding no longer leaves a floating bar far above small heads. Team marker follows the bar; building bars unchanged.
- Confirmed projectile faults: live shot could retain a visual Dictionary removed by the effects timer; legacy close-range ranged attacks incorrectly selected melee presentation; online snapshots directly displayed stationary positions even when flight time was shorter than 0.20s. Expired active visuals reattach, all real ranged attacks emit effects, each flight/bounce has endpoints and a local playback clock, and repeated snapshot IDs cannot replay. Native monotonic IDs use a constant-space watermark per match; trajectory effects still time out after termination or settlement.
- Shared projectile drawing uses a dark rim, bright core, short trail and a brief terminal spark above ground level, with visible-head culling. Warm/cool projectile tint follows alliance, including 3v3 and FFA. Physical projectile speed, damage timing, stable targeting, bounce counts and piercing hits stay in the existing simulation.
- Independent read-only review by combat_patch_review caught two handoff boundaries: visual sequence must survive authority migration, and the pre-existing client snapshot receive sequence must reset to match the server's new authority sequence. Snapshots now carry the visual serial even when no effects survive; observers reset receive sequence and a new authority restarts packet sequence. Tests use isolated callbacks/constructed snapshots, not a live room or cloud deployment.
- Existing isolated regressions passed on final runtime: Animal Table V3 3788/0, Combat Feedback 302/0, Flat Hex View 4679/0, Dog/Rewards/Gacha 43/0, Online Rewards Release 50/0. Some older/headless fixtures log ObjectDB cleanup warnings; no script errors. Indentation and diff whitespace checks pass.

## Verified closure

- New standalone fixture: headless 10340 checks / 0 failures; actual Vulkan Forward+ GPU 10348 / 0 (eight additional image saves). New GPU log has no script/parse errors or cleanup warnings. The main agent inspected battle, gacha and deck PNGs at 720x1280 and scaled 360x640, plus successive projectile frames; correct dot scope, overhead gaps and visible advancing trails confirmed. Engine import/parse and GDScript indentation pass.
- The fixture covers all 10 configured ranged animals, long-frame removal/reattachment, equal simultaneous trajectories, target death, two bounces, piercing, settlement cleanup, tower and restored legacy ranged paths; 60 animal art-bound combinations through zoom/rotation/mirroring plus transparent/source-frame bounds; actual six-direction normal melee pursuit, all four jumping animals attacking adjacent units/buildings, fixed grid landings and visible contact lunge.
- Networking checks include reordered numeric IDs [10,12,11], repeated/expired packets, lower IDs in a new match, constant-space numeric watermark, serial 9000 inheritance through an empty-effects snapshot, new authority's first accepted projectile and observer acceptance of sequence 1 after migration. These are isolated engine behavior tests; no external/live-room, Android-device, server deployment or release-package claim.
- Evidence: temp/qa/combat-readability-20260915/{headless,gpu,animal-table,combat-feedback,flat-hex,dog-gacha,reward-release,parse}.log and eight PNGs. Test writer returned both files; independent read-only final review passed; shared task lock released. Test data/output remain excluded from Git/runtime assets. No CSV/export, approved workbook, account data or unrelated artifacts changed. No new APK requested or exported in this scope.
