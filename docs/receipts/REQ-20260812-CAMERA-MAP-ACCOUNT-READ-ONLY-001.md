# REQ-20260812 制作人组合交付只读回执

- 请求：`REQ-20260812-PRODUCER-CAMERA-MAP-ACCOUNT-APK`
- 状态：`READY FOR IMPLEMENTATION`
- 控制级别：`L3`；按项目禁令不创建新代理，由当前 `codex-primary` 单写者执行并保留独立里程碑复核。
- RAG gate：`temp/rag/receipts/rag-gate.json`，索引签名 `171268c72551752b3cf25c8028a4dcd68dc58152aaae912636b2870b4bd86afa`。
- 任务上下文：`temp/rag/receipts/tasks/REQ-20260812-PRODUCER-CAMERA-MAP-ACCOUNT-APK.json`，9 条可验证引用。
- 正式规则：`docs/CURRENT_GAME_DESIGN.docx` 第 14 节；版本控制同步源 `docs/CURRENT_GAME_DESIGN.md`。

## 基线

- 5 张经典 `1V1` 地图在 50 个种子下双方直接接壤公共边只有 `4/6/4/4/4`；双方各 36 格、3 格中立。
- 配对中线配额方案可把 5 张地图稳定提升到至少 `9/12/9/9/9` 条接壤边，同时保持双方格数相等、各自连通、双方新增站点模板一致、到基地距离差不超过 2，并保留 1 格中立。
- 当前镜头只有平移偏移，没有缩放；世界坐标、点击反算和拖动边界都按 `1.00` 投影。
- 项目已有用户名密码注册、登录、会话、安装绑定、自动恢复、服务器档案保存与账号中心入口；仍需以本地闭环、失败回包和现有公网服务只读探针确认，不把仅有源码视为完成。
- `production/deployment/aliyun-profile.yaml` 缺少目标、所有者、备份、回滚和远程写授权，因此本任务不执行远程部署或重启；只允许对当前配置 endpoint 做只读探针。
- `project.godot`、`knowledge/*` 的既有工作区修改不属于本任务，必须保留且不得整文件覆盖。

## 接受条件

1. 轴向战斗世界默认 `1.30` 投影；世界到屏幕再反算无偏差，拖动和点击仍命中正确世界对象，战斗数值不变。
2. 5 张经典地图跨至少 50 个种子均达到双方公共边不少于 9、双方格数相等、双方领地连通、至少 1 格中立、新增战线格到各自基地距离差不超过 2；同种子的多人 `create_match()` 输出不变。
3. 用户名密码注册登录、错误密码拒绝、会话与安装绑定、重启自动恢复、档案持久化通过行为测试；客户端不保存明文密码。
4. Android 包为固定竖屏、具有 Internet 权限、使用项目当前云服务器地址并通过导出与产物哈希验证。
