# Home and gacha v1.1 filed producer direction

REQ-20260925-HOME-GACHA-POLISH, F-ZC-HOME-001 and F-ZC-GACHA-NEW-001. Producer confirmed 2026-09-25: ring 1..8 determines the fixed building visual tier. There is NO building upgrade gameplay, button, cost or transaction.

## Approved changes
- Remove all persistent connection/debug/status banners and the minus/plus/reset camera controls from home. Retain direct map dragging and reasonable gesture navigation; remove old hit targets. No retry-connection button. Failures may use a short actionable toast after a player action, never a permanent overlay.
- Locked plots have a neutral uncolored hex and only a simple readable type silhouette plus the existing coin price. No colored terrain or preview building art. Tapping a locked plot directly attempts unlock if adjacent and affordable, otherwise shows the relevant short failure message; it never opens locked-building detail. Owned buildings still expose daily-function details.
- When zooming or panning hides a locked plot's price, the first tap focuses that plot and reveals its readable price without spending. Its next tap uses the same direct unlock action. Never buy a plot while its price is clipped or hidden.
- Generate original simplified consistent home building sprites: four types x eight fixed ring tiers, 32 distinct images. Ring1 is the simplest. The single initial castle retains its identity but receives a distinct home sprite; it has no level progression. No repeated/recolored substitute building images across tiers. Do not alter daily yield, ring prices, inventory or authoritative transactions for this visual tier.
- Buildings dominate owned plots, including ring1, larger than the previous 110px transparent canvas. Fit visible alpha bounds rather than whitespace; increase visible occupancy from tier1 to8 while staying inside the hex roads and keeping residents readable. Type/numerical information must not cover buildings.
- Every first-owned animal, all four rarities, must run rarity (tap or2s) then hero close-up. Diagnose normal gacha path without changing odds/grants or modifying real user ownership. Existing all-owned accounts are not to be reset or treated as new. Repeated animals do not trigger a false new-hero ceremony.
- Replace harsh draw/reveal audio with softer cues and a distinct warm surprise motif for new heroes. Avoid stacked high-frequency transients in ten-pulls. Verify event routing and provide actual audio evidence.
- Gacha result cards show card artwork/name/quality only, without level badges, fragment counts, upgrade progress or upgrade affordances. This change does not remove level information from deck or animal details.

## September 25 readability correction

REQ-20260925-HOME-GACHA-READABILITY supersedes the conflicting display rules above. Producer supplied four runtime screenshots and explicitly requested implementation. Adjacent locked plots retain type and price during panning; draw them continuously behind the opaque top/bottom UI, without center-inset visibility switches. Nonadjacent locked plots show the neutral hex only. Full price readability still gates direct purchase; hidden/clipped-price first taps focus without spending, and nonadjacent taps never focus as purchasable. Owned buildings also move continuously behind the UI masks.

The owned-building panel keeps building identity, with one daily-output heading above item cells. Remove capacity/activity prose, duplicate item names and rate text from this compact panel; show official item icon and quantity on each cell. Preserve next-day availability as a short status. Item quality uses expected daily exchange value: below 15 green, 15..39 blue, 40..69 purple, 70+ gold. This display-only classification uses existing value/config data and does not change rewards. The same value bands apply to reward-list item cells, splitting different qualities of the same item where needed.

The home navigation red dot means claimable daily rewards only. After a successful claim it disappears immediately, including when gold can afford additional adjacent plots. Pending/failed claim keeps the dot if rewards remain claimable; the next day's claimable snapshot restores it.

Gacha results reuse the original rounded collection-card visual style and proportions. Hide level/progress/upgrade indicators in results, enlarge the contained animal image, keep the name, and remove color/quality labels. No stretching of artwork; flip animation preserves proportions of the resting card. Collection gameplay and upgrades remain unchanged. Producer additionally requests a clean new-hero reveal: remove explanatory text and countdown below the card, and quality/color labels throughout the ceremony. Tap or the existing timeout starts an actual card-opening flip before revealing the large animal. Hero celebration keeps the animal/name and continue action, with detailed statistics and ability explanations remaining in collection details. Prevent click-through and repeated audio/grants during the flip.

Runtime acceptance: frozen failing baseline and same probe after repair, mouse/touch drag across all map edges, no purchase on drag, adjacency changes after unlock, poor/disabled/next-day states, quantity/quality consistency, single and ten results, 720x1280 and 360x640 native rendering. Current implementation-only scope; no server change or package export requested.

## September 25 spacing, emoji and animal slogan amendment

REQ-20260925-GACHA-2332 includes REQ-20260925-HOME-EMOJI and REQ-20260925-ANIMAL-SLOGANS. Explicit producer requests supersede the earlier enlarged result artwork and handdrawn-face-only emotion rules. Ten results form four centered rows of 2, 3, 3, 2, with the two middle rows offset slightly left/right. Keep existing 132x158 rounded cards and 24px names; use 40px horizontal and 28px vertical gaps. Extend the results panel and move its existing draw buttons together above navigation. Reduce animal visible artwork to 78% of its prior contained fit, without distortion; tower artwork, collection and battle sizing remain unchanged.

Home animals show a small rounded ivory dialogue bubble above the head with a short pointer and exactly one actual emoji. Map happy/love/excited/tired to smiling/heart-eyes/star-eyes/sleeping emoji using bundled licensed artwork; no text, handdrawn imitation face or platform font dependency. Preserve existing mood triggers, duration, routes and hidden states; leave a visible gap above the walking animal.

Every animal has one short original humorous Chinese slogan in the authoritative cards.csv `slogan` field, exported through the current schema into cards.json. Building cards leave the optional field empty. Configuration validates a maximum of 32 characters and a single authored line; longer values or embedded newlines are rejected before export. Display the configured line below the animal and name during the hero stage of the first-acquisition ceremony, within a restrained dialogue panel using the existing palette. Reference-image influence is limited to the character-and-dialogue hierarchy; do not copy its assets, words or historical-war skin. Keep title, animal proportions, continue button and reveal sequence. Use legible 30px text, at most two display lines, no shrink-to-fit or ellipsis. Missing/blank slogans omit the panel safely. Rarity and flip stages contain no slogan. Slogans do not change abilities, grant behavior, odds or duplicate-animal rules.

Runtime acceptance: same frozen before/after evaluators, native mouse and touch, all animals and four moods, 720x1280 and 360x640. Existing single/ten draw economy, flip and first-owned queue regressions remain required. Source, native runtime and exported device evidence remain separate; no APK or server update requested.

## Prior v1.1 scope and acceptance history

Same Godot4.6.2, 720x1280 and360x640. Root owns home UI, home visual config/rules, docs/state and home acceptance. Gacha owner exclusively owns main.gd, gacha reveal, audio and gacha tests while writing. Art owners exclusively own their named building PNG sets and provenance manifests. Preserve preexisting dirty workbook and unrelated files. Producer subsequently authorized updating the existing Alibaba Cloud staging game server to repair affordable construction. Deploy the verified existing server source with only home transactions, wallet persistence and compatible HomeRpc, preserving the existing account and battle behavior. Back up the ELF and authority before a graceful restart; validate with an isolated synthetic account and never modify the producer's holdings. Production, APK and persistent local servers remain outside scope. Require normal input before/after reproduction, four-rarity first-acquisition checks, unchanged reward economy, actual runtime screenshots and generated-art alpha/hash/occupancy proof; release all write sets before commit/push. Primary workflow orchestrator/control-plane, implementation godot-feature-slice-implementation, art imagegen.
