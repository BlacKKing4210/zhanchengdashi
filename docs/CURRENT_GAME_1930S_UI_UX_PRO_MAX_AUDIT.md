# 占城大师 1930s UI 效果图 ui-ux-pro-max 审计

日期：2026-07-10

阶段：纯效果图，禁止实装

Skill 来源：`https://github.com/nextlevelbuilder/ui-ux-pro-max-skill/tree/main/.claude/skills/ui-ux-pro-max`

## 1. 使用确认

本轮实际安装并运行了 `ui-ux-pro-max`，不是只在流程文档中引用名称。

- 本机路径：`C:\Users\76398\.codex\skills\ui-ux-pro-max`
- 设计系统查询：`portrait mobile strategy card battle game playful vintage 1930s hand-drawn skeuomorphic information-dense`
- UX 查询：`mobile game touch navigation resource counters modal loading disabled locked text readability layout shift safe area`
- 风格查询：`vintage retro hand-drawn skeuomorphic playful game consistent components`
- 色彩查询：`entertainment game vintage warm paper red green blue high contrast`
- 字体查询：`mobile game readable display body numbers Chinese playful vintage`

## 2. 游戏化取舍

Skill 的第一次综合推荐包含 Bento Grid、3D/Hyperrealism 和深色界面。这些建议与已经确认的 1930s 轻松手绘方向、当前页面 UE 和移动游戏读图需求冲突，因此不采用。

本项目采用以下结果：

- `Sketch Hand-Drawn (Mobile)`：暖纸张、手绘边框、有限的不规则感、硬偏移阴影和一致组件语法。
- 中文可读字体：标题保留手写/楷体气质，正文、数值和状态使用清晰中文字体，不用只支持拉丁字符的装饰字体承担信息。
- 信息层级：每页只有一个最强主 CTA；次操作、取消和不可用状态必须明显降级。
- 动态数字：金币、票券、战力、价格和计时器使用稳定槽位并视觉居中，位数变化不改变周边布局。
- 状态清晰：selected、pressed、disabled、locked、loading、empty、error 和 unaffordable 不依赖单一颜色表达。
- 移动可用性：主要点击目标的视觉/预期点击区不小于 44 x 44 px，相邻目标保留至少 8 px 间隔。
- 跨页一致性：标题横幅、资源条、分区条、卡框、按钮、底部导航和弹窗使用统一边框、色彩角色和材质层级。
- 弹窗层级：背景只用于退后，弹窗标题、对象/结果、主操作和次操作按单一路径阅读。

不采用以下 Web/SaaS 特定建议：

- Bento 页面结构、网页 Hero、浮动操作按钮和桌面 hover 作为主交互。
- 3D/WebGL、多层视差、玻璃拟态和复杂模糊阴影。
- 原生系统控件外观直接覆盖项目的手绘游戏视觉。
- 为适配 skill 而新增、删除或移动当前页面控件。

## 3. v14 Design Tokens

| Token | 值 | 用途 |
| --- | --- | --- |
| `ink` | `#301E16` | 正文、数值、边框内线 |
| `paper` | `#FFFADE` | 高可读纸张底色和描边反差 |
| `paper-muted` | `#F2DBA6` | 次级面板和禁用底色 |
| `primary-red` | `#AC372D` | 主操作外框、危险或强提醒 |
| `secondary-blue` | `#26708B` | 次操作、信息状态和选中辅助 |
| `success-green` | `#2A734A` | 已完成、可用和正向状态 |
| `accent-gold` | `#CA801F` | 主 CTA、奖励和强调数字 |
| `disabled-wash` | `#766551`，约 38% 叠加 | 不可购买/禁用组件，不改变组件边界 |
| `outline` | 3-4 px 深墨线 | 保持 1930s 手绘组件统一 |
| `touch-min` | 44 x 44 px | 效果图阶段的最小可点击表达 |
| `touch-gap` | 8 px | 相邻操作的最小视觉间隔 |

关键文本组合的对比度：

- `ink` / `paper`：15.10:1。
- `paper` / `primary-red`：5.99:1。
- `paper` / `secondary-blue`：5.30:1。
- `paper` / `success-green`：5.47:1。
- `ink` / `accent-gold`：5.00:1。

## 4. v14 效果图改动

- 主页面：保持全部坐标和动物 PNG；资源数字继续居中；主操作保持唯一最强 CTA；战斗入口选中态增加更明确的描边/下划提示。
- 编组页：状态条和分区标题改用同一套手绘票券组件，减少简单圆角块与背景风格的割裂。
- 战斗页：保持 `648 x 1038` 外框、`592 x 982` 内场和 `7 x 13` 共 91 格；不改动物源图和格位坐标。
- 抽卡页：`抽 10 次`为主 CTA，`抽 1 次`为次操作；两者边界不移动。
- 商店页：余额不足商品增加整体禁用洗色，并保留文字原因；刷新操作保持单一主 CTA。
- 更多/任务页：完成状态使用“已完成”文字加 success 角色，不只靠颜色；设置、公告、邮件作为次操作，领取全部为主 CTA。
- 弹窗：购买为主 CTA、取消为次操作；背景只做轻微退后处理；卡牌详情与胜利弹窗保持一个明确出口。
- 全页面：标题、正文、数字、卡片、导航和弹窗状态统一；不改动物图片，不新增页面模块。

## 5. v14 QA Gate

- 九张独立页面均为 `720 x 1280`，另有 3 x 3 总览。
- 所有保留文字可辨认，无伪字、乱码、边框遮挡和低对比度丢失。
- 顶部金币/票券数字在各自数值栏中心，动态位数不会挤压图标。
- 战斗页检测到 91 个固定格位，外框不侵入有效内场。
- 主/次/禁用/完成/选中状态至少同时使用文字、明度、描边或材质中的两种表达。
- 原动物文件不修改，效果图继续直接合成项目现有 PNG。
- 只允许修改 `docs/`、`output/visual_concepts/`、`output/pdf/` 和效果图生成工具。
- 用户明确说“实装”前，不修改 `scripts/`、`scenes/`、运行时 `assets/`、输入逻辑或点击区域。

## 6. 实际执行结果

本轮最终输出前共完成三轮内部验收：

1. Producer / UI 首轮：主次 CTA、导航选中、任务完成和弹窗层级通过；商店禁用态洗色范围过大，记为 P1。
2. Art Director 复验：禁用洗色收回卡片内芯，P1 清零；洗色边缘仍略规则，记为 P2。
3. UI / QA 最终轮：改为柔和褪色纸张处理，保留手绘卡框；P0、P1 清零，P2 关闭。

量化结果：

- 10 张独立 PNG 均为 `720 x 1280`；总览为 `920 x 1980`。
- v14 战斗页与 v13 战斗页 SHA-256 一致，证明棋盘、91 格、单位位置和战斗区没有漂移。
- v14 大厅背景与 v13 大厅背景 SHA-256 一致，只调整信息/状态表现，不改场景构图。
- `assets/card_art/animals/`、`scripts/`、`scenes/` 和 `runtime/` 没有产生本轮差异。
- 五组关键文本/背景 token 对比度均达到或超过 4.5:1。
- 两个生成器通过 Python 语法检查，全部输出可重复生成。

## 7. Skill 解决的问题

`ui-ux-pro-max` 在本轮解决的是 UI/UX 质量问题，不负责生成 1930s 场景插画或重绘动物：

- 把“看起来不够专业”拆成 design tokens、组件一致性、信息层级、状态矩阵和 QA 项目。
- 明确主/次 CTA，并让 selected、completed 和 unaffordable 同时使用文字与视觉线索。
- 将简单圆角状态块替换为项目内已有的手绘票券组件，改善 UI 与背景风格割裂。
- 统一中文字体、数字槽位、对比度和弹窗阅读顺序。
- 以尺寸、哈希、目录差异和复验记录证明 UE/资产未漂移；skill 不生成 1930s 背景或动物，只负责 UI 设计与质量控制。

## 8. v15 重新验收基线

用户复审指出 v14 仍有较多细节问题，因此撤销“可冻结”的内部结论。v15 从待整改状态重新进入最新 UI/UX 流程，继续只制作效果图。

本轮再次运行 `ui-ux-pro-max` 的 design-system、UX 和 style 查询。综合推荐中的 Web/Bento、夸张极简和绿粉配色仍与本项目冲突，继续拒绝；采用以下移动端规则：

- `Sketch Hand-Drawn (Mobile)` 的暖纸张、深墨线、有限色彩和一致硬阴影。
- 触控目标不小于 `44 x 44 px`，相邻目标至少 `8 px`；效果图不修改现有点击区和 UE 坐标。
- 固定元素不得遮挡，保留文字必须在装饰安全区内完整显示。
- 页面只保留一个最强 CTA；次操作使用独立材质，不再由主按钮简单降饱和得到。
- selected、disabled、completed 和 unaffordable 至少使用两种线索表达。
- 数值使用稳定槽位并居中；正文对比度目标不低于 `4.5:1`。

### 8.1 冻结项

- 不修改动物 PNG、动物身份、阵容信息或卡牌数量。
- 不新增、删除、移动、重命名或重排页面控件、入口和信息区。
- 战斗页保持 `7 x 13`、91 格、`592 x 982` 内场、`648 x 1038` 外框及当前单位坐标。
- 不修改 Godot 场景、脚本、运行时资源、输入逻辑或点击区域。

### 8.2 v14 缺陷清单

| 严重度 | 缺陷 | v15 处理 |
| --- | --- | --- |
| P1 | 小状态条由完整段位条强行压缩，端头变形且文字挤压 | 改为保角九宫格短绶带，图标端、文字安全区和尾饰分别保留比例 |
| P1 | 次级按钮由金色主按钮简单降饱和，呈灰紫色且跨页不统一 | 建立独立青绿纸券次按钮，保留手绘纹理和清晰层级 |
| P1 | 抽卡分页使用简单圆角绿块，与整套手绘组件割裂 | 改为项目已有手绘分页条，不再绘制简单几何底块 |
| P1 | 商店不可购买态像矩形贴片，遮挡商品内芯 | 只柔和褪色商品图，原因放入原价格带，不覆盖卡框 |
| P2 | 底部导航同时使用红字、下划线和大圆点，选中态重复且贴近屏幕边缘 | 保留红字和原插槽内小徽记两种线索，删除下划线和大圆点 |
| P2 | 编组卡牌名称、角色文字和装饰圆章距离过近 | 文字回到各自安全区，角色文字避开左侧圆章 |
| P2 | 弹窗正文、奖励和按钮基线不完全统一 | 三类弹窗采用同一垂直网格和操作基线 |

### 8.3 v15 QA Gate

1. 逐页以 `720 x 1280` 原尺寸和 50% 总览两种尺度检查文字、边框、对齐和遮挡。
2. 顶部资源数值按图标右侧有效纸张内框居中，允许三位数测试时不碰装饰端头。
3. 所有短绶带端头不拉伸，文字不进入图标圆章和尾饰安全区。
4. 主、次、不可用和选中状态在九张页面中使用同一组件语法。
5. v15 战斗页必须与 v14 战斗页 SHA-256 一致。
6. 动物目录、`scripts/`、`scenes/`、`runtime/` 和 `project.godot` 不产生本轮差异。
7. Producer -> Art Director -> UI Artist -> UI Programmer -> QA Lead 连续复验，P0/P1 清零后才提交用户审核；该结论仍不等于允许实装。

## 9. v15 实际执行与终验

v15 没有沿用 v14 的内部“通过”结论，而是按原尺寸逐页重做并经历多轮不通过、修改和复验。

### 9.1 组件与页面修正

- 状态/分区绶带：改用卡牌源件的保角九宫格，独立保留左圆章、正文安全区和右尾饰；不再压缩完整段位条。
- 按钮：金色主 CTA 与青绿次 CTA 使用两套独立手绘材质；禁用按钮使用褪色纸券和明确文字。
- 导航：选中态只保留红色标签和原插槽内小徽记，不再叠加下划线与屏幕边缘大圆点。
- 编组/抽卡/商店：动物和商品图不重绘，只把内容中心对回现有卡框中心；四字状态和三位数价格预留至少 20 px 尾饰区。
- 抽卡/商店操作区：恢复 v14 的按钮外接框；分页恢复原 `124 x 28` 指示槽，在原槽内用尺寸和双环表达选中。
- 商店数据：以 60 金币为同一场景基准；80/120/180/300/680 商品使用褪色图像和红色价格，20 刷新与 5 券箱保持可用。
- 任务：票券使用轮廓遮罩清除源图矩形残片；进度和完成状态回到正文安全区。
- 弹窗：卡牌详情、战斗胜利、确认购买分别叠加在编组、战斗、商店页面；卡牌详情依据现有 UI/UE 文档补回关闭入口，碎片不足时禁用升级。
- 弹窗边框：底边改用顶部边框镜像保角，消除卡牌角色绶带被横向拉长的问题。
- 信息文字：纸张上的价格、奖励、碎片和胜利奖励统一使用 `ink #301E16`；金色只用于图标、装饰和大按钮材质。
- 大厅背景：独立背景输出移除六个动物占位阴影；完整大厅继续使用原动物 PNG，动物本身不修改。

### 9.2 UE 锁定证据

- 抽卡按钮恢复为 `(118,1001,326,1075)` 与 `(394,1001,602,1075)`。
- 商店刷新按钮恢复为 `(260,1027,460,1089)`。
- 抽卡分页恢复为 `(298,950,422,978)`。
- 三个弹窗面板保持 `(90,370,630,780)`，标题区和操作基线保持一致。
- 战斗页继续直接复制锁定稿；v14 与 v15 SHA-256 均为 `37e75a41c504759035073bcb234f869f7880d6610299f62adca19cfb76ecd6cc`。
- 战斗页保持 `7 x 13`、91 格、`592 x 982` 内场、`648 x 1038` 外框及单位坐标。

### 9.3 量化结果

- 9 张页面、1 张独立大厅背景均为 `720 x 1280`。
- 总览为 `1280 x 2260`；每张页面严格缩放为 `360 x 640`，即 50%，无裁切。
- `ink #301E16` / `paper #FFFADE` 对比度为 15.10:1；纸面信息不再使用低对比金色正文。
- 顶部金币/票券仍使用固定数值槽；三位数测试不会进入图标或端头区域。
- `assets/card_art/animals/`、`scripts/`、`scenes/` 和 `runtime/` 没有本轮差异。
- `project.godot` 在 v15 工作开始前已处于用户修改状态，时间早于本轮输出；本轮不修改、不回退、不纳入效果图提交。
- 两个生成器通过 Python 语法检查，整套输出可重复生成。

### 9.4 多角色复验记录

1. 首轮：压扁绶带、灰紫次按钮、重复导航选中态、简单分页和矩形禁用贴片记为 P1，不通过。
2. 第二轮：底部操作区出现规则纸张补丁、卡内中心偏移、源件相邻圆章残片，P1 未清零。
3. 第三轮：分隔条压住最近获得名称/商店价格、任务票券带矩形残片、弹窗底边横向拉伸、信息金色对比不足，P1 未清零。
4. Producer 复验：发现按钮外接框漂移、弹窗上下文错误及页面数据矛盾，回退到 UE 锁定坐标并统一场景状态。
5. Art Director / UI Artist / UI Programmer / QA Lead 复验：P0=0、P1=0；50% 总览、战斗哈希、弹窗基线和禁用状态通过。
6. 最终整改：纸面信息全部改为深墨，四字状态与三位数价格进一步左移，关闭最后两项产品 P1。

当前结论：**v15 纯效果图可提交用户审核，P0=0，P1=0；仍然禁止实装。** 冻结动物 PNG 带来的白色贴纸边属于已知 P2，按用户要求不在本轮修改。

## 10. ui-ux-pro-max 使用结论

本轮确实使用了已安装的 `ui-ux-pro-max`。它没有生成 1930s 插画，也没有修改动物，而是解决以下问题：

- 将“小问题很多”拆成组件一致性、触控尺寸、信息层级、状态矩阵、动态数字槽位、对比度和固定元素防重叠。
- 识别并拒绝不适合本项目的 Web/Bento、夸张极简和绿粉综合推荐，只采用 `Sketch Hand-Drawn (Mobile)` 与移动 UX 规则。
- 用 44 px 最小目标、8 px 间距、4.5:1 对比度、单一主 CTA、禁用/选中双线索和稳定数字槽位形成可复验标准。
- 通过逐页原尺寸、精确 50% 总览、哈希、坐标和目录差异证明视觉重做没有漂移战斗与运行时。

![v15 终验总览](../output/visual_concepts/current_game_1930s_v15_m_uiux_pixel_polish_page_set_overview.png)
