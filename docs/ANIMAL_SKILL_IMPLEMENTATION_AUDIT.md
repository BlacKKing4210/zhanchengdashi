# 动物技能实装验收

更新日期：2026-07-19

## 2026-07-19 卡表权威来源确认

- 用户更新后的 `config/tables/cards.csv` 是属性和技能描述的唯一设计来源；导出前必须保持 UTF-8，运行时只读取 `runtime/config/cards.json`。
- `skill_text` 非空时，玩家界面只能原样显示该字段；`skill_text` 为空时，不显示技能、无技能、速度或结构化技能兜底文本。
- 空描述不等于停用结构化战斗效果。产品验收必须分别核对“是否显示”和“是否实际触发”，不能用自动生成的文案掩盖两者差异。

## 2026-07-19 本轮修正

- 已将用户保存的 `cards.csv` 从 GBK 无损转换为 UTF-8，完成配置校验和 `runtime/config/cards.json` 导出；65 张运行时卡牌字段与表格逐项一致。
- 13 张空 `skill_text` 卡牌（含青蛙、兔子、鸽子、鸭子、骆驼、鹤和四座防御塔）在编组详情、战斗地块详情和建筑摘要中不再显示技能行。
- 描述优先的行为已补齐：死亡/占领金币、击杀者成长、建筑/远程双倍伤害、25%死亡成长、全队速度和生命光环、概率攻击成长、-2 伤害减免、剧毒每秒最大生命值 50% 伤害、远程击杀金币，以及银背猩猩召唤的无技能限制。

## 产品负责人结论

当前动物技能不能判定为“全部已按描述实装”。卡牌表中同时存在结构化技能字段与旧版自然语言描述，部分卡牌的两者含义冲突；运行时又会根据描述是否为空选择不同分支，因此存在描述正确但效果走旧逻辑、结构化配置存在但未执行、效果生效但缺少可见反馈三类问题。

## 本轮 P0 修正

- 鸭：明确为“阵亡时，在原位置召唤 1 只鸭子”。死亡返场时在新鸭子身上显示召唤反馈，避免与普通兵营召唤混淆。
- 界面中的结构化召唤技能文案按触发器显示，不再把阵亡返场写成“召唤时额外召唤”。

## 全量检查规则

每张动物卡必须同时通过以下检查才可标记为已实装：

1. `skill_trigger` 有对应的运行时触发入口。
2. `skill_effect` 在该触发入口中有处理分支。
3. `skill_power` 与 `skill_cooldown_sec` 被实际读取，或文档明确说明固定值。
4. 卡牌详情描述与实际触发时机、目标、数值一致。
5. 玩家能在受影响动物身上看到图标与数值/状态反馈。

## 当前风险分组

- 结构化路径已存在：登场护盾/攻击/生命/速度/眩晕，攻击减速/眩晕/伤害/护盾/斩杀，受伤治疗/护盾/反伤，阵亡金币/召唤，占领金币，周期金币/治疗/护盾/修复。
- 需要继续专项验收：`aura` 光环的结构化数值一致性、`copy` 的统一定义、旧文案驱动的击杀/受伤/目标优先级技能，以及每个技能的冷却是否按配置执行。
- 无结构化技能的卡牌不等于无被动；其自然语言被动仍需逐张迁移后才能可靠验收。

## 2026-08-13 群体属性增益召唤快照修正

- 全量扫描出六张会增加己方全体动物攻击、最大生命或移动速度的卡：羊、狐狸、天鹅、马、斑马、长颈鹿。
- 六张卡统一使用 `skill_trigger=on_spawn` 与 `group_buff` 标签；结构化效果和强度与 `skill_text` 对齐，周期冷却清零。
- 每次召唤技能动物时，仅对当时仍存活的己方所有动物结算一次，并包含技能动物自身；敌方不受影响。
- 周期刷新不再扫描或重新施加永久群体属性。之后召唤的动物不补发旧增益；再次召唤技能动物时才按新的在场名单新增一次快照。
- 随机友军、阵亡触发、局部范围、护盾和临时状态技能不属于本次群体快照集合，保留原逻辑。
- 回归矩阵逐张覆盖配置归类、自身、既有友军、敌方排除、四次定时刷新、后来召唤、第二次技能动物召唤及按次叠加。
