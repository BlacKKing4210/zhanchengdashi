# Historical audit entry point now runs the complete current producer contract.
# Permanent spawn snapshots and old duck death summons were superseded by V3.
extends "res://tests/test_animal_table_v3.gd"
